<UserControl x:Class="mitoSoft.homeNet.ArduinoIDE.Views.DocumentionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:p="clr-namespace:mitoSoft.homeNet.ArduinoIDE.Properties">
    <DockPanel>
        <ToolBar DockPanel.Dock="Top">
            <Button x:Name="PrintButton" 
                    Content="{x:Static p:Resources.Button_PrintExport}"
                    Padding="10,2,10,2"
                    Click="PrintButton_Click"/>
        </ToolBar>

        <ScrollViewer x:Name="MainScrollViewer"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Auto"
                      Background="White">
            <StackPanel x:Name="ContentPanel" Margin="10"/>
        </ScrollViewer>
    </DockPanel>
</UserControl>
